machine:
  node:
    version: 0.12.0

general:
  branches:
    ignore:
      - gh-pages

test:
  pre:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
  override:
    - npm run test-circleci  > $CIRCLE_TEST_REPORTS/junit/xunit.xml
